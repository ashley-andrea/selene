# Inference server dependencies — simulation model
# Keep this slim: only what serve.py needs at runtime
# NOTE: numpy must be >=2.0 — model artifacts were pickled with numpy 2.x;
#       loading them with numpy 1.x raises ValueError on PCG64 BitGenerator.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.0
scikit-learn==1.6.1
numpy==2.2.6
pandas==2.2.3
joblib==1.5.2
